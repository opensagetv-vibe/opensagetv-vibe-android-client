v0.5.49 restores the exact proven Search text timing after intermittent runs showed the Fire TV keyboard visible but the SageTV Search field empty.

Normal startup now uses:
  command search
  delay 50
  adb input text <required --text>
  delay 50
  Android BACK
  delay 100
  ff
  right
  play_pause
  down
  select

The debug hasTextInput/IME signals are not allowed to delay text injection. They may be captured after injection for diagnostics only.

dev_type_text now distinguishes Android command completion from actual field-content verification.

No Android source changed; no APK rebuild is required.
