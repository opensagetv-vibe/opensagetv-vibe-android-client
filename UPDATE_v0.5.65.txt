SageTV MiniClient Dev v0.5.65

Purpose
- Make Android player-fix iteration faster by rerunning only cases/operations that were abnormal in the previous matrix.
- Record an evidence-ranked Android player review from 20260827_232446_player_full_matrix.json.

New matrix selector
- `--issues-only` uses the built-in v0.5.65 known-issue profile.
- `--issues-only <report.json>` derives the focused case/check set from any previous player-matrix JSON.
- Startup-only failures are retained even though they have no post-startup checks.
- `--checks`, `--case-id`, player/streaming/decoder filters still intersect the issue set.

Current focused hardware Push/Pull set
- 9 configurations
- 16 media operations
- 2 startup-only GSY/System cases

Android review
- Six watchdogs are isolated to direct local seek on Media3-family Push. The server-driven Push FLUSH/anchor/reprepare path recovers, so no speculative production Push behavior change is included.
- Media3 Pull/GSY-Media3 Comskip repeatedly consumes exactly 50,069,504 additional bytes (191 x 256 KiB reads) and then remains in BUFFERING long after network reads stop; controlled extractor/seek-map experiments are recommended.
- GSY/System Pull reports Android MediaPlayer what=1 / extra=-2147483648; Push also never reaches prepared playback. A System-backend data-source compatibility investigation is recommended before behavioral refactoring.

APK
- Host/test/docs only. debugStatusVersion=10 remains current; no rebuild is required solely for v0.5.65.
