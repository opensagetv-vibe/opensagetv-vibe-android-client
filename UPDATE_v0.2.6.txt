SageTV MiniClient Dev v0.2.6

Fixes the Firebase-removal Logger.java compile failure against the real ILogger interface.

Apply over v0.2.5, then run:
  ./dev.sh test
  ./dev.sh validate
  ./dev.sh build

No Docker image rebuild is required.
The "Keystore properties file missing" message is for the optional release keystore path and is not the debug-build failure shown here.
