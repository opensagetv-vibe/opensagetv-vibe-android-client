SageTV MiniClient Dev v0.5.50

Fix: repeated/full player-matrix runs could show the Fire TV keyboard but drop a whole fast recording-title `adb input text` phrase.

Changes:
- paced Android OS text injection, one character at a time
- spaces sent as KEYCODE_SPACE
- default 100 ms between characters
- configurable with --text-char-delay-ms (0..2000)
- applied to mcp-playback-test, mcp-media3-matrix, mcp-media3-comskip-matrix, and mcp-player-matrix
- legacy one-shot sendtext remains available for diagnostics

No Android APK/source change; rebuild is not required.
