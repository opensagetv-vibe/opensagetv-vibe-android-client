SageTV MiniClient Dev v0.5.35

Android debug receiver compile repair.

Fixes:
- Restore DevTestReceiver.configuredValues(PrefStore).
- Replace the missing appendPlaybackHealth(...) helper call with the existing PlaybackHealthProbe snapshot/compactWire path.
- Add static regression checks for both pieces.

No playback algorithm, startup policy, Comskip command, or Pull diagnostics behavior is changed.
Rebuild/reinstall the debug APK.
