# Matrix Review — 20260828_010837

Source report: `20260828_010837_player_full_matrix.json`
Project baseline: v0.5.66 / debugStatusVersion 10
Mode: `--issues-only`, fresh playback per operation

## Summary

- executionPassed: true
- infrastructureFailures: 0
- recovered: 4
- slow recovery: 5
- watchdog expired: 5
- player crashed: 0
- media not recovered: 0
- startup playback failures: 2 (GSY/System Pull and Push)
- watchdog: 50 seconds

The issue-isolation harness is now behaving correctly. No poisoned-session `INFRA_ERROR` results occurred.

## Key finding 1 — Media3 Pull 4x TS search did not improve the large forward Comskip

Latest large forward Comskip recoveries:

- native Media3 Pull: 35,044 ms
- GSY Auto/Media3 Pull: 35,534 ms
- GSY Media3 Pull: 41,606 ms

The 4x run therefore does not improve the prior repeatable ~33–34 second behavior.

### Native Media3 Pull I/O delta

Before seek:

- network bytes: 17,301,504
- READ count: 66
- cumulative READ wait: 2,391 ms
- open count: 3

After recovery:

- network bytes: 66,846,720
- READ count: 255
- cumulative READ wait: 6,965 ms
- open count: 5

Operation delta:

- 49,545,216 bytes
- 189 READs
- ~4,574 ms additional READ wait
- 2 additional opens

This is almost the same ~50 MB post-seek data churn seen with the earlier 8x setting.

### Stronger decoder/preroll evidence

At the exact seek callback:

- target: 985,220 ms
- video rendered: 721
- video queued input: ~380

At the 15-second long-wait probe:

- datasource counters already reached their final values
- player still BUFFERING
- video rendered: 721 (no new output)
- video queued input: 1046

At the 30-second probe:

- datasource counters unchanged from 15 seconds
- player still BUFFERING
- video rendered: 721
- video queued input: 1632

First post-seek video frame appears only around queued-input 1722.

Interpretation: the current leading hypothesis is no longer simply “large TS timestamp-search buffering.” Media3 appears to have completed the network/read phase and then spends tens of seconds feeding/processing a large MPEG-2 preroll or decode-only backlog before reaching a renderable sync point.

## Key finding 2 — Exo2 Pull now needs datasource telemetry before any player fix

Native Exo2 Pull Comskip-right:

- target: 985,220 ms
- status: WATCHDOG_EXPIRED
- recovery: 50,579 ms
- no crash
- player enters BUFFERING
- video queued/rendered counters stop advancing after the seek

But Exo2 Pull datasource metrics are all `-1`, so the report cannot separate server/socket starvation from extractor/player/decoder stalling.

Next action: instrument Exo2 Pull with the same READ/open/position timing used by Media3 Pull, then rerun only this check.

## Key finding 3 — Push watchdogs remain direct-local seek behavior, not proven SageTV server seek failures

Native Media3 Push and GSY Media3 Push absolute/backward local seeks still watchdog.

During these local seeks:

- `serverRequestedSeekMs` remains `-1`
- server seek sequence does not increment
- existing flush/anchor sequence remains unchanged
- player locally enters BUFFERING and output counters stop

This continues to prove only that file-style local `player.seekTo()` is unreliable against the current Push source. It is not proof that SageTV's normal server-driven FLUSH/rebase path is broken.

Next action: capture one genuine SageTV-originated Push seek end-to-end.

## Key finding 4 — GSY/System remains a separate Android MediaPlayer problem

Pull startup still produces:

`player_error_1_-2147483648`

Push creates an `android.media.MediaPlayer` and receives server FLUSH/anchor/play events, but never becomes ready.

Neither case crashes the app.

Next action: add `MediaDataSource` `readAt` / `getSize` / EOF / exception telemetry and compare the same recording as a local file through Android `MediaPlayer`.

## Current recommended controlled sequence

1. Restore Media3 Pull TS timestamp search to the known 8x baseline before changing another behavior variable.
2. Add datasource-open history around Pull seek operations.
3. Add decoder preroll/decode-only/skip/drop counters where available.
4. Run a single-variable Pull sync-point experiment (`NEXT_SYNC` versus current `CLOSEST_SYNC`) and validate seek-to-zero plus both Comskip directions.
5. Instrument Exo2 Pull datasource timing before changing Exo2 playback behavior.
6. Add correlated SageTV server evidence collection.
7. Capture a genuine server-driven Push seek.
8. Add device/API/effective MediaCodec adapter telemetry.
9. Continue GSY/System datasource/platform isolation last.

Core verdict rule remains:

> A/V recovery is the verdict. Seek accuracy is telemetry.
