SageTV MiniClient Dev v0.3.7 -> v0.3.8

Purpose: rollback internal player telemetry hooks after clean-install testing confirmed they prevent video startup on the Fire TV AFTMM.

Preserved:
- DEV001 fixed SageTV client ID
- Dynamic fresh-install streaming mode
- clean Gradle build
- uninstall-before-install
- Docker/ADB/MCP external diagnostics

After extracting over the project root:
  ./dev.sh test
  ./dev.sh validate
  ./dev.sh build
  ./dev.sh install
  ./dev.sh launch

Verify video playback first.
