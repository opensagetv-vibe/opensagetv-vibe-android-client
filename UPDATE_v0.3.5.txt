SageTV MiniClient Dev v0.3.4 -> v0.3.5

Fixes validation after the intentional v0.3.4 rollback to logcat-only Android telemetry.
No Docker image rebuild is required.
No APK source files are changed by this patch.

Extract over the existing SageTV-MiniClient-Dev project root and overwrite files.
Then run:
  ./dev.sh validate
  ./dev.sh test
  ./dev.sh build
  ./dev.sh install
  ./dev.sh launch

For the first runtime check, verify video playback only.
