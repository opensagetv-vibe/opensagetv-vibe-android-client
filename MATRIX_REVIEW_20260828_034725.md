# Player tuning matrix review — 20260828_034725

## Summary

- 27 requested combinations: 3 Push + 24 Pull.
- Push: `sync` 3.768 s, `auto` 4.967 s, `async` 7.103 s.
- Pull: 21 valid observations; all were slow recovery or watchdog. Three additional Pull combinations were invalid startup/infra cases.
- No tested Pull knob fixes the long Comskip-right stall.

## Pull factor summary

| Dimension | Result |
|---|---|
| TS search 8x | median ~35.8 s; 34.3–38.1 s; no watchdogs |
| TS search 4x | median ~38.0 s; 23.5–51.1 s; 3 watchdogs |
| CLOSEST_SYNC | median ~35.4 s |
| NEXT_SYNC | median ~37.5 s |
| 256 KiB reads | median ~35.5 s |
| 512 KiB reads | median ~37.5 s |
| codec auto | median ~35.4 s |
| codec sync | median ~35.5 s |
| codec async | median ~37.5 s |

## Conclusions

1. Keep Media3 Pull 8x as the stable baseline, but do not expect it to solve the stall.
2. Do not promote NEXT_SYNC, 512 KiB Pull reads, or async codec mode from this run.
3. Push `sync` is promising and should be repeated against `auto` before becoming a default.
4. Fix tuning-matrix startup contamination before another broad sweep. `pull_combo_004` reached EOF/delete prompt before measurement.
5. Move the next Pull experiment to SageTV server NIO OFF/ON and a controlled small SageTV recovery skip after stall detection.
